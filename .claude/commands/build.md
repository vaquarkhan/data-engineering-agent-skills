# /build

Select the matching platform preset from `presets/` and the relevant execution skill from `skills/`, then implement in small slices.

Typical combinations:

- `warehouse-and-schema-design` + `dbt-and-analytics-engineering`
- `spark-and-distributed-processing` + `apache-spark-engineering`
- `streaming-and-messaging-systems` + `apache-flink-stream-processing`
