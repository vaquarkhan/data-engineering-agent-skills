# /spec

Use `skills/data-specification/SKILL.md` and the templates in `templates/` to define the data product before implementation.

Checklist:

1. identify the business outcome
2. identify source and destination contracts
3. define grain, cadence, quality, privacy, and replay expectations
4. list open questions instead of guessing
