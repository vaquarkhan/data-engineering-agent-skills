# Asset Registry

This directory contains machine-readable indexes for repository assets.

Current files:

- `assets.json`

Use the registry when you need to:

- discover starter packs, examples, templates, MCP templates, or installer bundles
- validate that installer surfaces stay aligned across tools
- build future CLIs or plugin flows without scraping Markdown docs
