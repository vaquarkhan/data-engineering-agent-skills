# Claude Plugin Packaging

This directory contains template packaging metadata for bundling this repository as Claude-oriented plugin groups.

Suggested plugin groups:

- `data-engineering-core`
- `data-engineering-platforms`
- `data-engineering-adoption`

Use these manifests as a repository-local packaging layer if you want to publish skill bundles through a plugin marketplace workflow.
